# Recycling Scanner

A prototype computer vision app for detecting waste items and mapping them into public-facing categories such as Recycling, Trash, or Other.

## Setup

1. Create and activate a virtual environment
2. Install dependencies
3. Run the app:

```bash
python app/ui_app.py